application:
  id: 'HyperX_API_App'
  version: 1.0.0

spring:
  application:
    name: API Application
  profiles:
    # process env (local,dev,prod) 설정
    # active: dev
    # core 설정 연동
    include: core

  servlet:
    multipart:
      max-file-size: 10MB

  config:
    import:
      - classpath:application-prometheus.properties
      - classpath:application-apidoc.yml
      - classpath:application-jwt.yml

  main:
    web-application-type: reactive
    allow-bean-definition-overriding: true

#logging:
#  config: classpath:logback-spring.xml
#management:
#  tracing:
#    enabled: true
#    sampling:
#      probability: 1.0
#  endpoints:
#    web:
#      exposure:
#        include: '*'
#logging:
#  pattern:
#    correlation: "[${spring.application.name:},%X{traceId:-},%X{spanId:-}] "
#  include-application-name: false
#otel:
#  exporter:
#    otlp:
#      endpoint: "http://localhost:4317"

# 로컬 환경 설정
---

spring:
  config:
    activate:
      on-profile: local
    import:
      - classpath:application-r2dbc-local.properties
      - classpath:application-log-local.yml

# 개발계 환경 설정
---

spring:
  config:
    activate:
      on-profile: dev
    import:
      - classpath:application-r2dbc-dev.properties
      - classpath:application-log-dev.yml
# 운영계 환경 설정
---

spring:
  config:
    activate:
      on-profile: prod
    import:
      - classpath:application-r2dbc-prod.properties
      - classpath:application-log-prod.yml